# 현재 프로젝트에 AI로 서버와 통신 하는 기능 추가
1. 언리얼 서버로 시작하면 웹서버에 서버아이피 등록하는 기능
2. 클라이언트로 로그인하면 등록 서버 아이피를 받아서 접속할 수 있게 하는 기능

# 필수 규칙
* .으로 시작하는 폴더와 파일 (.claude, .codex, .cursor .vsconfig 등)은 꼭 필요한 상황이 아니면 탐색하지 않아야 한다.
* Binaries, DerivedDataCache 폴더는 디버깅에 필요한 상황이 아니면 탐색하지 않는다.
* 6. 언리얼엔진은 호출 할때 c++로만 작업해줘.
